# 学习进度

> 记录各概念域的掌握程度。每次练习后更新，用于后续针对性出题。

---

## 01 基础

状态：**未练习**

| 知识点 | 掌握 | 备注 |
|--------|------|------|
| — | — | — |

---

## 02 提示词Prompt

状态：**已完成练习**（2026-06-30）

综合掌握：🟡 基本掌握，有几个薄弱点需巩固

### 强项
- `PromptTemplate.from_template` 基础用法
- `ChatPromptTemplate` 元组写法 `("system", "..."), ("human", "...")`
- `ChatPromptTemplate` 类写法（`SystemMessagePromptTemplate` / `HumanMessagePromptTemplate`）
- `MessagesPlaceholder` 插入历史消息
- `partial` 预填变量

### 薄弱点
| # | 问题 | 严重程度 | 出现题目 |
|---|------|----------|----------|
| 1 | Python `{}`（set）vs `[]`（list）混淆——新手常见 | 🔴 阻断 | 题4 |
| 2 | `PromptTemplate` vs `ChatPromptTemplate` 选型边界不清——简单字符串模板不该用 Chat 版 | 🟡 概念 | 题5 |
| 3 | 审题不细——题2要求 `to_messages()` 遍历打印类名，却用了 `to_string()` | 🟡 习惯 | 题2 |
| 4 | Import 随意——导了 `langchain_classic`（不存在）/ 未使用的模块 | 🟢 习惯 | 题1 |

### 出题建议
后续出题时：
- 重点出 **Python 集合类型** 相关的坑（`{}` 空花括号是 dict 不是 set，`{1,2}` 才是 set）
- 多出需 **区分 PromptTemplate / ChatPromptTemplate** 的场景
- 适当加入 **读取题目要求细节** 的陷阱（比如指定用某个方法但留了捷径）
- 暂时不需要再出基础 from_template / from_messages 的纯记忆题

---

## 03 输出解析

状态：**未练习**

---

## 04 LCEL与Runnable

状态：**未练习**

---

## 05 记忆Memory

状态：**未练习**

---

## 06 工具与Agent

状态：**未练习**

---

## 07 检索增强RAG

状态：**未练习**

---

## 08 流式与回调

状态：**未练习**

---

## 09 工程化与可靠性

状态：**未练习**

---

## 10 评估

状态：**未练习**
